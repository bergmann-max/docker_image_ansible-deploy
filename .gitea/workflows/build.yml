---
# How to trigger this workflow:
#   1. Commit and push your changes to main:
#        git add .
#        git commit -m "your message"
#        git push origin main
#   2. Create a version tag (must start with "v"):
#        git tag v1.2
#   3. Push the tag to Gitea:
#        git push origin v1.2
#   → The workflow will start automatically and build/push the image
#     as "ansible-deploy:1.2" and "ansible-deploy:latest"
#
# Prerequisites:
#
# Action Secrets:
#   DOCKER_TOKEN  - Docker Hub Access Token
#
# Action Variables:
#   REGISTRY_USER - Docker Hub username
#   IMAGE_NAME    - Docker image name


name: Build docker image ansible-deploy

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-dev:
    if: github.ref_type == 'branch'
    runs-on: ubuntu
    container:
      image: docker:dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
    steps:
      - name: Install Node.js
        run: apk add --no-cache nodejs npm

      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3

      - name: Login to Docker Hub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          username: ${{ vars.REGISTRY_USER }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push dev
        run: |
          docker build -t ${{ vars.REGISTRY_USER }}/${{ vars.IMAGE_NAME }}:dev .
          docker push ${{ vars.REGISTRY_USER }}/${{ vars.IMAGE_NAME }}:dev

  build-release:
    if: github.ref_type == 'tag'
    runs-on: ubuntu
    container:
      image: docker:dind@sha256:686d2c5464787b0ca42420e0aa03a94fc9781ff9bf8b5ff947614b9fa4c3c3e0
    steps:
      - name: Install Node.js
        run: apk add --no-cache nodejs npm

      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3

      - name: Extract version from tag
        run: echo "VERSION_TAG=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          username: ${{ vars.REGISTRY_USER }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push release
        run: |
          docker build \
            -t ${{ vars.REGISTRY_USER }}/${{ vars.IMAGE_NAME }}:${{ env.VERSION_TAG }} \
            -t ${{ vars.REGISTRY_USER }}/${{ vars.IMAGE_NAME }}:latest \
            .
          docker push ${{ vars.REGISTRY_USER }}/${{ vars.IMAGE_NAME }}:${{ env.VERSION_TAG }}
          docker push ${{ vars.REGISTRY_USER }}/${{ vars.IMAGE_NAME }}:latest
